import { world } from "@minecraft/server";
import { ActionFormData } from "@minecraft/server-ui";

/**
 * 
 * @param {Player} player 
 */
export async function showMain(player){
    const main = new ActionFormData()
    
}
